{
  "name": "portfolio-dashboard-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix client install && npm --prefix server install",
    "build": "npm --prefix client run build && rm -rf server/public && mkdir -p server/public && cp -r client/dist/* server/public/ && npm --prefix server run build",
    "start": "npm --prefix server start"
  }
}
